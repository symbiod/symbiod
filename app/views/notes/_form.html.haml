= simple_form_for note, url: url do |f|
  = f.input :content, label: t('dashboard.notes.content'), input_html: { rows: 10, 'data-provide': 'markdown' }
  = f.submit t("dashboard.notes.buttons.#{submit}"), class: 'btn btn-info'
