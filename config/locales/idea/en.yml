en:
  idea:
    sessions:
      submit: Submit
      access:
        deny: The user is already logged in
    registrations:
      submit: Sign up
      access:
        deny: The user is already logged in
    proposes:
      create: Create
      access:
        deny: The user already has an propose
    landing:
      go_to_dashboard: Go to dashboard
      go_to_proposes: Go to propose
      new_propose: New propose
      author_log_in: Authtor Log In
